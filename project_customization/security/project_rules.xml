<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="project_visibility_follower_rule" model="ir.rule">
            <field name="name">Project: Only Followers Can See</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="domain_force">
                [('privacy_visibility', '=', 'followers'),
                ('message_partner_ids', 'in', [user.partner_id.id])]
            </field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/> <!-- Apply to internal users -->
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="rule_project_admin_full" model="ir.rule">
            <field name="name">Project: Full Access for Admins</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <!-- always true -->
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="perm_read" eval="True"/>
        </record>

        <record id="rule_project_l3_manager_only" model="ir.rule">
            <field name="name">Project: L3 Managers See Own Projects</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="groups" eval="[(4, ref('project.group_user_l3'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
        </record>
    </data>
</odoo>