<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_leave_type_form_inherit_out_duty" model="ir.ui.view">
        <field name="name">hr.leave.type.form.out.duty</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_status_form"/>
        <field name="arch" type="xml">

            <!-- Place checkbox in General Information section -->
            <xpath expr="//field[@name='requires_allocation']" position="after">
                <field name="is_out_duty"/>
                <field name="is_od_comp_off"/>
            </xpath>

        </field>
    </record>

</odoo>
